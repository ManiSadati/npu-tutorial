# pto-isa row-softmax baseline

From this folder run `bash run.sh` to build, simulate and check the 10×1024
FP32 output. Edit `src/kernel.cpp` for rewrites. `bash run.sh --no-build` skips
compilation. See [the baseline guide](../README.md) for setup, tensor files,
comparison commands, and validation tolerances.
