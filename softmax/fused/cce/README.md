# Fused CCE softmax

Run `bash run.sh` here to build, simulate and check the output using the baseline
Python comparison. See [the fused guide](../README.md) for the repair equations,
exact kernel diff size, and validation details.
